# 🚀 Инструкция по запуску в Opera GX

## ✅ Самый простой способ

1. Откройте папку `content-ai-agent` в проводнике Windows
2. Дважды кликните на файл **`START_OPERA.bat`**
3. Проект автоматически запустится и откроется в Opera GX

## 📋 Альтернативные способы

### Способ 1: Через командную строку

1. Откройте командную строку (cmd)
2. Перейдите в папку проекта:
   ```cmd
   cd E:\ИИшка\content-ai-agent
   ```
3. Запустите:
   ```cmd
   python -m streamlit run streamlit_app.py
   ```
4. Откройте Opera GX вручную и перейдите по адресу: **http://localhost:8501**

### Способ 2: Через Python скрипт

1. Откройте командную строку
2. Перейдите в папку проекта:
   ```cmd
   cd E:\ИИшка\content-ai-agent
   ```
3. Запустите:
   ```cmd
   python quick_start.py
   ```

## 🔧 Если что-то не работает

### Проблема: Порт 8501 занят

Закройте другие приложения, использующие порт 8501, или измените порт:
```cmd
python -m streamlit run streamlit_app.py --server.port 8502
```

### Проблема: Opera GX не открывается автоматически

1. Запустите Streamlit вручную (см. Способ 1)
2. Откройте Opera GX вручную
3. Введите в адресной строке: `http://localhost:8501`

### Проблема: Модули не найдены

Убедитесь, что вы находитесь в папке `content-ai-agent`:
```cmd
cd E:\ИИшка\content-ai-agent
```

И установите зависимости:
```cmd
pip install -r requirements.txt
```

## ✅ Проверка

Если все работает, вы увидите:
- В терминале: "You can now view your Streamlit app in your browser"
- В Opera GX: интерфейс Content AI Agent

## 🛑 Остановка

Для остановки сервера:
- Нажмите `Ctrl+C` в терминале
- Или закройте окно терминала

