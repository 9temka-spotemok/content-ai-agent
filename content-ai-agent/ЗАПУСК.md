# 🚀 Как запустить проект в Opera GX

## Способ 1: Через batch файл (самый простой)

1. Откройте папку `content-ai-agent`
2. Дважды кликните на файл `run_opera.bat`
3. Проект автоматически откроется в Opera GX

## Способ 2: Через командную строку

```bash
cd content-ai-agent
python -m streamlit run streamlit_app.py --server.headless true --server.port 8501
```

Затем вручную откройте Opera GX и перейдите по адресу: http://localhost:8501

## Способ 3: Через Python скрипт

```bash
cd content-ai-agent
python launch_opera.py
```

## ⚠️ Если Opera GX не открывается автоматически

1. Запустите Streamlit вручную:
   ```bash
   cd content-ai-agent
   python -m streamlit run streamlit_app.py
   ```

2. Откройте Opera GX вручную

3. Перейдите по адресу: http://localhost:8501

## 🔧 Проверка

Если сервер запущен, вы увидите в терминале:
```
You can now view your Streamlit app in your browser.
Local URL: http://localhost:8501
```

